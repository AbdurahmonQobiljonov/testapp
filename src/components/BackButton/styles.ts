import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  header: {
    flexDirection: 'row',
    alignItems: 'center',
    columnGap: 8,
  },
  text: {
    fontSize: 18,
    fontWeight: '600',
  },
});

export default styles;
